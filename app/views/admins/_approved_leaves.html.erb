<table class="responsive-table">
  <thead>
    <%= render 'sign_offs/sign_off_list_table_head' %>
  </thead>
  <tbody>
  <% if @sign_offs[:approved].present? %>
    <%= render partial: 'sign_off_list', locals: {sign_offs: @sign_offs[:approved] } %>
    <% end %>
  </tbody>
</table>