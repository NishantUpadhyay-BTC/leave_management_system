<table class="responsive-table">
  <thead>
    <%= render 'sign_offs/sign_off_list_table_head' %>
  </thead>
  <tbody>
    <% if @sign_offs[:rejected].present? %>
    <%= render partial: 'sign_off_list', locals: {sign_offs: @sign_offs[:rejected] } %>
    <% end %>
  </tbody>
</table>